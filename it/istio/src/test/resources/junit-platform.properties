# JUnit Platform Configuration for Istio Tests
# Enable parallel test execution by default
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.mode.classes.default=concurrent

# Configure thread pool for parallel execution
junit.jupiter.execution.parallel.config.strategy=fixed
junit.jupiter.execution.parallel.config.fixed.parallelism=2

# Tests using @ResourceLock will still run serially
# This ensures proper isolation for Istio cluster operations